import type { ReactNode } from "react";

// TODO: implement — centered-card shell for auth/verify/confirm.
// See docs/DESIGN_SYSTEM.md, "StandaloneShell".
export function StandaloneShell(props: { children: ReactNode }) {
  return props.children;
}
