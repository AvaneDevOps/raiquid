import type { NavItem } from "@/lib/nav-config";

// TODO: implement. See docs/DESIGN_SYSTEM.md, "Bottom tab bar (mobile nav)".
export function BottomTabBar(props: { items: NavItem[] }) {
  return null;
}
