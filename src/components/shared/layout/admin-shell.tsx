import type { ReactNode } from "react";

// TODO: implement — top-tab shell for /admin. See docs/DESIGN_SYSTEM.md, "AdminShell".
export function AdminShell(props: { children: ReactNode }) {
  return props.children;
}
