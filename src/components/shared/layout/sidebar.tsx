import type { NavItem } from "@/lib/nav-config";
import type { SessionUser } from "@/components/shared/layout/session-user";

// TODO: implement. See docs/DESIGN_SYSTEM.md, "Sidebar (desktop nav)".
export function Sidebar(props: { items: NavItem[]; user: SessionUser }) {
  return null;
}
