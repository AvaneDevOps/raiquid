// TODO: implement (or delete once every route is built for real).
// See docs/ROUTE_MAP.md.
export function ScreenPlaceholder(props: {
  screenNumber: string;
  screenName: string;
  route: string;
}) {
  return null;
}
